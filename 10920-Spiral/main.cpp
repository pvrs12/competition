#include <iostream>
#include <cmath>

using namespace std;

int main() {
	while (true) {
		int sz, p;
		cin >> sz >> p;
		if (!sz && !p) {
			break;
		}
		//top right is square of grid size
		//in from top right is ((rows from top right)/2)^2

		//find square sz,p is in
		for(int i=sz;i>=1;--i){

		}
	}
}
